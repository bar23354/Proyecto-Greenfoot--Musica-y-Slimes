import greenfoot.*;

public class Hitbox extends Actor {
    // The Hitbox class can be left empty as it only serves as a marker for the attack range

    // Constructor that accepts x and y coordinates as arguments
    public Hitbox(int x, int y) {
        setLocation(x, y);
    }
}

